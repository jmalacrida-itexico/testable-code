package com.itexico.main.testableCode.examples.original

import spock.lang.Specification

class InvoiceGeneratorTest extends Specification {
    def "PrintOrEmailInvoice"() {

        given: "an invoice generator"
        //def container = Mock(Container)
        //def invoiceId = 1
        //def invoiceGenerator = new InvoiceGenerator(container, invoiceId)

        //container.database >> Mock(Database)

        when: "I print an invoice"


        then: "it is printed"

    }
}
